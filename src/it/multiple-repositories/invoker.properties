# For this test, we override the default invoker goals
# This allows us to maintain focused multi-repository tests without including all Maven plugin dependencies
# Instead of running dependency:resolve, we just compile
invoker.goals = clean compile
