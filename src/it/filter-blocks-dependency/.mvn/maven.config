# Filter is enabled by default when extension is registered
# Configure basedir since session.rootDirectory may not be available in invoker tests
-Daether.remoteRepositoryFilter.strict.basedir=${session.rootDirectory}/.mvn/remoteRepositoryFilters
