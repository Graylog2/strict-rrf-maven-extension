# Integration test configuration for allow-deny-rules
# Allow everything but deny a specific artifact to test deny rules
# We deny guava to test the deny functionality
repo.central.allow = *
repo.central.deny = com.google.guava:*
