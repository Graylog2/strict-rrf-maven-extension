# Test that quoted paths are properly handled
# This simulates the issue where maven.config includes quotes
-Daether.remoteRepositoryFilter.strict.basedir="${session.rootDirectory}/.mvn/remoteRepositoryFilters"
