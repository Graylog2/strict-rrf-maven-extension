# Very restrictive configuration - blocks all artifacts
# Only allows a specific non-existent namespace, effectively blocking everything
# This validates the fail-secure "default deny" behavior
repo.central.allow = internal.company.nonexistent.package


