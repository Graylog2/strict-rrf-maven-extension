# Integration test configuration for filter-disabled
# This config would block guava, but the filter is disabled so it won't apply
# Using a restrictive config to verify filter is truly disabled
repo.central.allow = org.slf4j
